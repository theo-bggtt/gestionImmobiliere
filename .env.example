# Copier en .env, puis remplacer les deux secrets par une valeur générée sur
# la machine — jamais reprise d'un dépôt, d'un README ou d'une PR :
#   openssl rand -hex 32
# (hexadécimal, et non base64 : le compose interpole POSTGRES_PASSWORD dans
# une URL, et le `/` de l'alphabet base64 la rend non analysable — les
# migrations échouent alors au démarrage sur un `TypeError: Invalid URL` qui
# ne désigne pas sa cause.)
#
# Ces deux secrets ne sont plus lus que par docker compose : depuis l'issue
# #35, les scripts de sauvegarde ne prennent de ce fichier qu'une liste blanche
# qui ne les contient pas. Guillemets et fins de ligne Windows n'y changent
# donc plus rien — ils divergeaient, et la divergence tombait à la RESTAURATION.
POSTGRES_DB=gestion_immobiliere
POSTGRES_USER=gestion
POSTGRES_PASSWORD=change-me
SESSION_SECRET=change-me-too-generate-a-long-random-string

# Hors Docker (dev local), DATABASE_URL pointe vers localhost.
# Dans docker-compose, l'app reçoit sa propre DATABASE_URL vers le service "postgres".
DATABASE_URL=postgres://gestion:change-me@localhost:5432/gestion_immobiliere
PORT=3000

# Racine du stockage des photos. En Docker, un volume monte sur /donnees.
STOCKAGE_RACINE=./donnees/fichiers

# ── Mise en service (README, « Mise en service ») ────────────────────────
# Nom de domaine servi par Caddy. `localhost` = certificat auto-signé d'essai.
DOMAINE=localhost

# Nombre de proxys devant le processus Node. Le compose pose 1 pour le
# conteneur (Caddy) ; hors Docker, laisser vide ou 0 : on ne se fie à personne.
PROXYS_DE_CONFIANCE=

# (Il n'y a PAS de variable d'inscription. L'inscription se ferme dès le
# premier compte, et ne se rouvre que par une invitation créée depuis
# `/proprietes/invitations` — un lien à jeton, qui expire et se révoque.
# `AUTORISER_INSCRIPTION` a été retirée : voir la décision #145.)

# Sauvegardes (scripts/sauvegarde.sh) : dossier local, rétention, et cible
# rsync hors de la machine. Vide = les sauvegardes ne quittent pas la machine,
# donc ne survivent pas à sa perte ; le script l'avertit à chaque passage.
SAUVEGARDES=./sauvegardes
RETENTION_JOURS=14
SAUVEGARDE_DESTINATION=
